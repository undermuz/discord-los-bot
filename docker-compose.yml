services:
  bot:
    build: .
    container_name: discord-los-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/bot.sqlite
    volumes:
      - ./data:/app/data
